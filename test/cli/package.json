{
  "name": "@noormme/cli-tests",
  "version": "1.0.0",
  "description": "Comprehensive CLI tests for NOORMME",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:cli": "jest --testPathPattern=cli",
    "test:unit": "jest --testPathPattern=unit",
    "test:integration": "jest --testPathPattern=integration"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0",
    "supertest": "^6.3.0",
    "@types/supertest": "^2.0.0",
    "mock-fs": "^5.2.0",
    "@types/mock-fs": "^4.13.0"
  }
}
